# =====================================================================
# LOL BP 预测与推荐系统 - 依赖清单
# =====================================================================
# 分组说明:
#   [core]    推理服务必需
#   [train]   训练 / 超参搜索 / 特征工程
#   [eda]     探索性数据分析 (可视化)
#
# 版本约束策略:
#   ~=X.Y  兼容锁定 (序列化模型文件加载, 跨大版本可能不兼容)
#   >=X.Y  仅设下限 (纯 Python 库, API 稳定)
# =====================================================================

# ---- [core] 推理服务必需 ----
numpy~=1.26
pandas~=2.2
torch~=2.4
transformers>=4.30
catboost~=1.2
lightgbm~=4.0
scikit-learn~=1.8
flask~=3.0
flask-cors>=4.0
python-dateutil>=2.8
pyarrow>=12.0
psutil>=5.9
pypinyin>=0.50

# ---- [train] 训练 / 超参搜索 (运行训练脚本时需要) ----
optuna>=3.4
scipy>=1.11

# ---- [eda] 探索性数据分析 (运行 EDA notebook 时需要) ----
matplotlib>=3.7
seaborn>=0.12
